@model IEnumerable<LojaDeConstrucao.Web.ViewsModels.UsuarioViewModel>
@{
    ViewData["Title"] = "Usuário";
}

<div class="row header">
    <div class="col-md-12">
        <h3>Users</h3>
        <a href="/Usuario/Create" class="btn btn-primary">Novo</a>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table table-hover">
            <tbody>
                @if(@Model != null)
                {
                    foreach(var viewModel in @Model)
                    {
                        <tr>
                            <td>
                                <a class="name">@viewModel.Email</a>
                            </td>
                            <td>
                                <a href="/UsuarioAlterarSenha/@viewModel.Id" class="btn">Alterar senha</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>